function hola() {
    var intentar = prompt('Estamos siendo ')

    if (intentar == 'We are being') {
        alert('Perfecto')

    } else {
        alert('Mal')
    }
}

function Adios() {
    var intentar = prompt('Hemos sido ')
    if (intentar == 'We have been') {
        swal('Perfecto')
    } else {
        swal('Mal')
    }
}

function SemPro() {
    var intentar = prompt('Estabamos')
    if (intentar == 'We were') {
        alert('Perfecto')
    } else {
        alert('Mal')
    }
}